apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scale-service.fullname" . }}-test-connection"
  labels:
    {{- include "scale-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scale-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
